---
import LegalDocument from '~/components/site/LegalDocument.astro';
import { getLegalForScope } from '~/utils/apps';
import { siteConfig } from '~/config/site';

/** The site-wide terms document, at its permanent address. */
const siblings = await getLegalForScope('site');
const doc = siblings.find(
  (r) => r.kind === 'terms' && r.locale === siteConfig.defaultLocale,
);

if (!doc) {
  throw new Error(
    'Missing src/content/legal/site/terms.' + siteConfig.defaultLocale + '.md. ' +
      'Either add it or remove src/pages/terms.astro.',
  );
}
---

<LegalDocument doc={doc} siblings={siblings} />
